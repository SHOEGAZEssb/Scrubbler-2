<UserControl
    x:Class="Scrubbler.Host.Presentation.Settings.AboutSettingsCategoryView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Scrubbler.Host.Presentation.Settings"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <StackPanel>

        <!-- Version row -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBlock Text="Version:"/>
            <TextBlock Text="{Binding CurrentVersion}" Margin="5,0,0,0"/>
        </StackPanel>

        <!-- Spacer -->
        <Border Height="5"/>

        <!-- Startup update checkbox -->
        <CheckBox Content="Check for Updates on start" IsChecked="{Binding EnableStartupUpdateCheck, Mode=TwoWay}"/>

        <!-- Spacer -->
        <Border Height="5"/>

        <!-- Check updates button -->
        <Button Content="Check for Updates" Command="{Binding CheckForUpdatesCommand}" HorizontalAlignment="Center"/>

    </StackPanel>
</UserControl>
