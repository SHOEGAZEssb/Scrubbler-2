<UserControl
    x:Class="Scrubbler.Host.Presentation.Settings.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Scrubbler.Host.Presentation.Settings"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:utu="using:Uno.Toolkit.UI"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <ScrollViewer Padding="12">
            <ItemsRepeater ItemsSource="{Binding Categories}">
                <ItemsRepeater.Layout>
                    <FlowLayout Orientation="Horizontal"/>
                </ItemsRepeater.Layout>

                <ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="local:SettingsCategoryViewModel">
                        <utu:CardContentControl Style="{StaticResource ElevatedCardContentControlStyle}">
                            <utu:CardContentControl.ContentTemplate>
                                <DataTemplate x:DataType="local:SettingsCategoryViewModel">
                                    <StackPanel Padding="32" Orientation="Vertical" HorizontalAlignment="Center">
                                        <TextBlock Text="{x:Bind Name}"
                                                   Style="{ThemeResource SubtitleTextBlockStyle}" HorizontalTextAlignment="Center"/>

                                        <ContentPresenter Content="{Binding}"
                                                          ContentTemplateSelector="{StaticResource ViewLocatorSelector}" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </DataTemplate>
                            </utu:CardContentControl.ContentTemplate>
                        </utu:CardContentControl>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </ScrollViewer>
    </Grid>
</UserControl>
