<UserControl
    x:Class="Scrubbler.Host.Presentation.Logging.LogView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:models="using:Scrubbler.Host.Models"
    xmlns:helper="using:Scrubbler.Host.Helper"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:log="using:Microsoft.Extensions.Logging"
    xmlns:local="using:Scrubbler.Host.Presentation.Logging">

    <UserControl.Resources>
        <helper:LogLevelToBrushConverter x:Key="LogLevelToBrushConverter"/>
    </UserControl.Resources>

    <Grid RowDefinitions="Auto,*">
        <StackPanel Orientation="Horizontal" Padding="8" Spacing="12" Grid.Row="0" HorizontalAlignment="Stretch">
            <TextBox Header="Filter log messages..." Width="300" Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

            <Border Width="1" Margin="8,0" Background="{ThemeResource DividerStrokeColorDefaultBrush}" />

            <TextBlock Text="Log Levels:" FontWeight="SemiBold" VerticalAlignment="Center"/>
            <ComboBox
                ItemsSource="{Binding LogLevelFilters}"
                IsEditable="False"
                IsTextSearchEnabled="False">

                <ComboBox.ItemTemplate>
                    <DataTemplate x:DataType="local:LogLevelFilterViewModel">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <CheckBox IsChecked="{Binding IsEnabled, Mode=TwoWay}" />
                            <TextBlock Text="{Binding Level}" Foreground="{Binding Level, Converter={StaticResource LogLevelToBrushConverter}}" FontWeight="Bold"
                                       VerticalAlignment="Center"/>

                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <Border Width="1" Margin="8,0" Background="{ThemeResource DividerStrokeColorDefaultBrush}" />

            <TextBlock Text="Module:" FontWeight="SemiBold" VerticalAlignment="Center"/>
            <ComboBox ItemsSource="{Binding Modules}" SelectedItem="{Binding SelectedModule, Mode=TwoWay}"/>

            <Border Width="1" Margin="8,0" Background="{ThemeResource DividerStrokeColorDefaultBrush}" />

            <Button Content="Clear Logs" Command="{Binding ClearCommand}" HorizontalAlignment="Right"/>
        </StackPanel>

        <Grid Grid.Row="1" RowDefinitions="Auto,*">
            <!-- Header row -->
            <Grid ColumnDefinitions="150,80,120,*" Background="#222" Padding="4" Row="0">
                <TextBlock Text="Timestamp" Foreground="LightGray" FontWeight="Bold"/>
                <TextBlock Text="Level" Grid.Column="1" Foreground="LightGray" FontWeight="Bold" HorizontalAlignment="Center"/>
                <TextBlock Text="Module" Grid.Column="2" Foreground="LightGray" FontWeight="Bold"/>
                <TextBlock Text="Message" Grid.Column="3" Foreground="LightGray" FontWeight="Bold"/>
            </Grid>

            <!-- Log entries -->
            <ListView ItemsSource="{Binding FilteredEntries}" Grid.Row="1">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:LogMessage">
                        <Grid ColumnDefinitions="150,80,120,*" Padding="4">
                            <!-- Timestamp -->
                            <TextBlock Text="{x:Bind Timestamp, Mode=OneWay}"
                                   Foreground="Gray"
                                   FontSize="12"/>

                            <!-- Level -->
                            <TextBlock Text="{x:Bind Level, Mode=OneWay}"
                                   Grid.Column="1"
                                   FontWeight="Bold"
                                   HorizontalAlignment="Center"
                                   Foreground="{x:Bind Level, Converter={StaticResource LogLevelToBrushConverter}}"/>

                            <!-- Module -->
                            <TextBlock Text="{x:Bind Module, Mode=OneWay}"
                                   Grid.Column="2"
                                   Foreground="LightBlue"
                                   FontStyle="Italic"/>

                            <!-- Message -->
                            <TextBlock Text="{x:Bind Message, Mode=OneWay}"
                                   Grid.Column="3"
                                   TextWrapping="WrapWholeWords"
                                   Foreground="White"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</UserControl>
