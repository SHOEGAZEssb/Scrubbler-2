<UserControl
    x:Class="Scrubbler.Plugin.ManualScrobbler.ManualScrobbleView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <StackPanel Spacing="8" Padding="12">
        <TextBox Header="Artist" Text="{Binding Artist, Mode=TwoWay}" />
        <TextBox Header="Track" Text="{Binding Track, Mode=TwoWay}" />
        <DatePicker Date="{Binding PlayedAt, Mode=TwoWay}" />
    </StackPanel>
</UserControl>
